import { TransactionHistory } from "@/components/transaction-history"

export default function TransactionsPage() {
  // Render the TransactionHistory component directly without additional headers
  return <TransactionHistory hideHeader />
}
