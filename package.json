{
  "dependencies": {
    "@supabase/supabase-js": "^2.50.2",
    "dotenv": "^17.0.0",
    "stripe": "^18.3.0",
    "swr": "^2.3.3",
    "zustand": "^5.0.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.1"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "type-check": "tsc --noEmit",
    "pre-push": "npm run type-check && npm run audit:production",
    "audit:production": "node scripts/production-readiness-audit-v2.js"
  }
}
